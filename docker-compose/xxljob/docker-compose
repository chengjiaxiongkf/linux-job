version: "3"
# 构建服务
services:
  # 第一个服务拉去jar包
  blade-xxljob:
    # 刚刚打包的镜像
    image: blade-xxljob
    ports:
      - "7009:7009"
    volumes:
      # 本地文件:映射容器文件
      - "./wait-for-it.sh:/wait-for-it.sh"
      - "./volumes/data:../data"